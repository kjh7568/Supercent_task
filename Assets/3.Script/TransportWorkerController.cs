using UnityEngine;

/// <summary>
/// 운반 인부 AI. PickupZone에서 아이템을 수집 후 DepositZone으로 운반한다.
/// S2에서 전체 구현 예정.
/// </summary>
public class TransportWorkerController : MonoBehaviour
{
    public void Init(PickupZone pickupZone, DepositZone depositZone)
    {
        // S2에서 구현
    }
}
